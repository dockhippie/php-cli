#!/bin/bash

checker() {
  if [[ $1 -ne 0 ]]
  then
    /bin/s6-svscanctl -t /etc/s6
  fi
}

/usr/bin/templater -d -p php \
  -o /etc/caddy/caddyfile \
  /etc/templates/caddyfile.tmpl
checker $?

chown -R caddy:caddy \
  /app
